import pacConfig from '../test/passing-configs/pac.development'
import validate from './index'

describe('index', () => {
  // it('works', () => {
  //   console.info('[index.test.js] pacConfig: ', pacConfig)
  //   const error = validate(pacConfig)
  //   console.info('[index.test.js] error: ', error)
  //   // throw error.details
  // })
})
